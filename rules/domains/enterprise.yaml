version: '1.0'
name: Enterprise Domain Rules
description: Enterprise IT, infrastructure, and DevOps security patterns
rules:
  - id: ssh_private_key_path
    name: SSH Private Key Path
    pattern: '(?:~|/home|/Users)/[^\s]*\.ssh/[^\s]*'
    enabled: false
    strategy: placeholder
    mode: detect
    priority: 70
    compliance: [SOC2, ISO_27001]
